@namespace Alife.Function.Speech
@inherits Alife.Framework.PluginUIBase<GenieSpeechModel, GenieSpeechModelConfig>
@using AntDesign
@using System.IO

<div style="background: #fff; padding: 20px; border-radius: 8px;">
    <Title Level="4" Style="margin-bottom: 24px;">Genie语音合成配置</Title>

    @if (!Directory.Exists(GenieSpeechModel.RuntimeFolder))
    {
        <Alert Type="AlertType.Warning"
               Message="Runtime/Genie 目录不存在"
               Description="请下载 GPT-SoVITS 模型文件并放置到 Runtime/Genie 目录下，否则语音合成功能将无法使用。"
               ShowIcon="true"
               Style="margin-bottom: 16px;"/>
    }
    else
    {
        <Alert Type="AlertType.Success"
               Message="Runtime 环境已就绪"
               ShowIcon="true"
               Style="margin-bottom: 16px;"/>
    }

    @DefaultUI
</div>
